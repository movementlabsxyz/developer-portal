import Events from '@/components/Events/Events';

export const metadata = {
  title: 'Events & Rewards | Movement Builder Kit',
  description: 'Join hackathons, community events, and get rewarded for building on Movement Network',
};

export default function EventsPage() {
  return <Events />;
}
